<h1 class="p-0 mt-5 text-center text-light">All Products</h1>

<div class="row">
  <div class="d-flex flex-wrap p-0" style="list-style: none;">
    <% @products.each do |product| %>
      <%= render partial: "shared/product_card", locals: {product: product} %>
    <% end %>
  </div>    
</div>